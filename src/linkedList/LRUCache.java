package linkedList;

public class LRUCache {
    public static void main(String[] args) {

        // we should use doubly linked list and hashmap, hashmap for search in constant time
        // and list for maintain the order and do in constant time,
        //so all search , insert and delete will perform in constant time


    }
}
